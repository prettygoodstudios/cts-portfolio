  <div class="card" data-id="<%= portfolio_item.id %>">
    <img src="<%= portfolio_img(portfolio_item.thumb_image, 'thumb') if !portfolio_item.thumb_image.nil? %>" style="max-height:300px;overflow:hidden;">
    <p class="card-text"><span><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item.id) %></span><%= portfolio_item.subtitle %></p>
  </div>
  <!--
  <p><%= portfolio_item.body %></p>
  <%= link_to "Edit Item", edit_portfolio_path(portfolio_item.id) if logged_in?(:site_admin)%>
  <%= link_to "Delete Item", portfolio_path(portfolio_item.id), method: :delete, data: { confirm: 'Are you sure?'} if logged_in?(:site_admin)%>
  
  -->